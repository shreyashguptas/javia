# Core dependencies (will use system packages if available via --system-site-packages)
requests>=2.32.0
python-dotenv>=1.0.0
numpy>=1.24.0
opuslib>=3.0.1
pyalsaaudio>=0.9.0

# OTA Update dependencies (must be installed in venv)
supabase>=2.23.0
uuid6>=2024.1.12  # Provides uuid7 functionality
pytz>=2025.1
realtime>=2.23.0

# Note: gpiozero is installed via system apt packages (python3-gpiozero)
# and accessed via --system-site-packages flag in venv

